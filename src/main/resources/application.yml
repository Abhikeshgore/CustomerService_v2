server:
  port: 8080
  servlet:
    context-path: /api/v2
spring:
  datasource:
    url: jdbc:h2:mem:test2
  h2:
    console:
      path: /h2
      enabled: true
      settings:
        web-allow-others: true
  jpa:
    show-sql: true

